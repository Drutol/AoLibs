<local:TestPageCBase
    x:Class="AoLibs.Sample.UWP.Pages.TestPageC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AoLibs.Sample.UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="using:AoLibs.Navigation.UWP.Pages"
    xmlns:viewModels="using:AoLibs.Sample.Shared.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:TestViewModelC}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Page C"/>
            <Button Content="NavigateAWithFirstOccurrence" Command="{Binding NavigateAWithFirstOccurrence}"/>
            <Button Content="NavigateBWithFirstOccurrence" Command="{Binding NavigateBWithFirstOccurrence}"/>
            <Button Content="GoBackCommand" Command="{Binding GoBackCommand}"/>
        </StackPanel>
    </Grid>
</local:TestPageCBase>
